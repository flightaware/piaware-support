#!/usr/bin/make -f
# -*- makefile -*-

export DH_VERBOSE=1

override_dh_systemd_enable:
	dh_systemd_enable --name=regenerate-ssh-keys

override_dh_systemd_start:
	dh_systemd_start --name=regenerate-ssh-keys

%:
	dh $@ --with=systemd
